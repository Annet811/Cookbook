<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ru.myitschool.vsu2021.tribunskikh_a_o.cookbook">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".EditRecipeDescriptionActivity"></activity>
        <activity android:name=".IngridientsActivity" />
        <activity android:name=".ListOfIngridientsActivity" />
        <activity android:name=".EditRecipeActivity" />
        <activity android:name=".ShowRecipeActivity" />
        <activity android:name=".ListOfRecipiesActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>